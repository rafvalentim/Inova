---
phase: 01-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/server.ts
  - apps/api/package.json
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
  - SEC-04
user_setup: []

must_haves:
  truths:
    - "Conexão Socket.IO sem accessToken válido (cookie ou query param) é recusada com mensagem de erro antes de qualquer handler ser invocado"
    - "Usuário autenticado sem membership no projeto não consegue fazer join na room daquele projeto"
    - "Os handlers de relay (task-updated, task-moved, task-created, task-deleted, comment-added) não existem mais no servidor"
    - "Respostas HTTP incluem headers X-Content-Type-Options, X-Frame-Options, Strict-Transport-Security"
  artifacts:
    - path: "apps/api/src/server.ts"
      provides: "Socket.IO auth middleware, room guard, relay removal, Helmet"
      contains: "io.use("
    - path: "apps/api/package.json"
      provides: "Helmet dependency"
      contains: "\"helmet\""
  key_links:
    - from: "apps/api/src/server.ts io.use() middleware"
      to: "apps/api/src/middleware/auth.ts jwt.verify()"
      via: "jsonwebtoken já instalado — reutilizar mesma lógica de verificação"
      pattern: "jwt\\.verify"
    - from: "apps/api/src/server.ts join-project handler"
      to: "prisma.projectMember.findFirst"
      via: "query antes de socket.join()"
      pattern: "projectMember\\.findFirst"
---

<objective>
Fechar os dois vetores de exfiltração via Socket.IO e adicionar headers de segurança HTTP.

Purpose: Qualquer cliente hoje pode conectar ao Socket.IO sem JWT e receber eventos de tasks em tempo real de qualquer projeto. Esta é a lacuna de segurança de maior risco no sistema — resolve SEC-01, SEC-02, SEC-03 (relay removal) e SEC-04 (Helmet).

Output:
- `server.ts` com `io.use()` middleware que verifica JWT e atribui `socket.data.userId`
- `server.ts` com `join-project` verificando membership antes de `socket.join()`
- `server.ts` sem os 5 handlers de relay de cliente (task-updated, task-moved, task-created, task-deleted, comment-added)
- `server.ts` com `app.use(helmet(...))` antes das rotas
- `apps/api/package.json` com `helmet` instalado
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/api/src/server.ts
@apps/api/src/middleware/auth.ts
@apps/api/src/config/index.ts
</context>

<interfaces>
<!-- Contratos relevantes extraídos do codebase. Não explorar — usar diretamente. -->

De apps/api/src/middleware/auth.ts:
```typescript
interface JwtPayload {
  userId: string;
  role: string;
}
// Verificação JWT: jwt.verify(token, config.jwt.secret) as JwtPayload
// config.jwt.secret vem de apps/api/src/config/index.ts
```

De apps/api/src/server.ts (estado atual):
```typescript
// Socket.IO setup — SEM io.use(), SEM verificação de membership
io.on('connection', (socket) => {
  socket.on('join-project', (projectId: string) => {
    socket.join(`project:${projectId}`); // sem verificação!
  });
  // relay handlers: task-updated, task-moved, task-created, task-deleted, comment-added
});

// app.set('io', io) já existe — routes podem acessar via req.app.get('io')
```

De apps/api/src/config/index.ts:
```typescript
export const config = {
  jwt: { secret: process.env.JWT_SECRET || 'dev-secret-change-me' },
  corsOrigin: process.env.CORS_ORIGIN || 'http://localhost:5173',
};
```

Prisma model ProjectMember:
```
@@unique([projectId, userId])  -- verificar com findFirst({ where: { projectId, userId } })
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Instalar Helmet e configurar no server.ts</name>
  <files>apps/api/package.json, apps/api/src/server.ts</files>
  <read_first>
    - apps/api/src/server.ts (ver linha exata onde middleware é registrado, antes das rotas)
    - apps/api/package.json (confirmar versão atual de dependências)
  </read_first>
  <action>
    1. Instalar helmet no workspace correto:
       ```
       cd apps/api && npm install helmet
       ```

    2. Em `apps/api/src/server.ts`, adicionar import no topo (junto com os outros imports):
       ```typescript
       import helmet from 'helmet';
       ```

    3. Adicionar `app.use(helmet(...))` APÓS `const app = express()` e ANTES de `app.use(cors(...))`.
       Usar esta configuração exata (API-only, sem HTML, Socket.IO compatível):
       ```typescript
       app.use(helmet({
           contentSecurityPolicy: false,         // API não serve HTML
           crossOriginEmbedderPolicy: false,     // compatibilidade Socket.IO
       }));
       ```

    ATENÇÃO: Não colocar após as rotas. Helmet deve ser o PRIMEIRO middleware de segurança.
  </action>
  <verify>
    <automated>cd /c/Users/RafaelValentim/Documents/Inova/apps/api && node -e "require('./dist/server.js')" 2>/dev/null || grep -n "helmet" src/server.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "import helmet" apps/api/src/server.ts` retorna linha com `import helmet from 'helmet'`
    - `grep -n "app.use(helmet" apps/api/src/server.ts` retorna linha com a configuração
    - `grep -n "contentSecurityPolicy: false" apps/api/src/server.ts` retorna resultado
    - `grep "\"helmet\"" apps/api/package.json` retorna linha de dependência
    - A linha `app.use(helmet(...))` aparece ANTES de `app.use(cors(...)` no arquivo (número de linha menor)
  </acceptance_criteria>
  <done>Helmet instalado e configurado como primeiro middleware; package.json atualizado</done>
</task>

<task type="auto">
  <name>Task 2: Socket.IO — JWT auth middleware + membership guard + remoção dos relay handlers</name>
  <files>apps/api/src/server.ts</files>
  <read_first>
    - apps/api/src/server.ts (ler o bloco completo do Socket.IO — linhas 27 a 70 aproximadamente)
    - apps/api/src/middleware/auth.ts (ver como jwt.verify é chamado e qual interface JwtPayload usa)
    - apps/api/src/config/index.ts (confirmar o campo jwt.secret)
  </read_first>
  <action>
    Em `apps/api/src/server.ts`, fazer as seguintes mudanças no bloco Socket.IO:

    **Passo A — Adicionar imports no topo do arquivo:**
    ```typescript
    import jwt from 'jsonwebtoken';
    import prisma from './config/database';
    ```
    (se ainda não existirem — verificar antes de adicionar)

    **Passo B — Adicionar io.use() middleware ANTES do io.on('connection'):**
    ```typescript
    // Middleware de autenticação Socket.IO (SEC-01)
    io.use(async (socket, next) => {
        try {
            // Token pode vir de cookie (preferido) ou query param (fallback para clientes que não suportam cookies com Socket.IO)
            const cookieHeader = socket.handshake.headers.cookie || '';
            const cookieMatch = cookieHeader.match(/accessToken=([^;]+)/);
            const tokenFromCookie = cookieMatch ? cookieMatch[1] : null;
            const tokenFromQuery = socket.handshake.auth?.token as string | undefined;
            const token = tokenFromCookie || tokenFromQuery;

            if (!token) {
                return next(new Error('Autenticação necessária: token não fornecido'));
            }

            const decoded = jwt.verify(token, config.jwt.secret) as { userId: string; role: string };
            socket.data.userId = decoded.userId;
            next();
        } catch {
            next(new Error('Token inválido ou expirado'));
        }
    });
    ```

    **Passo C — Substituir o handler join-project para incluir verificação de membership (SEC-02):**
    Remover o handler atual:
    ```typescript
    socket.on('join-project', (projectId: string) => {
        socket.join(`project:${projectId}`);
        console.log(`[Socket.IO] ${socket.id} joined project:${projectId}`);
    });
    ```
    Substituir por:
    ```typescript
    socket.on('join-project', async (projectId: string) => {
        try {
            const membership = await prisma.projectMember.findFirst({
                where: { projectId, userId: socket.data.userId },
            });
            if (!membership) {
                socket.emit('error', { message: 'Sem acesso a este projeto' });
                return;
            }
            await socket.join(`project:${projectId}`);
            console.log(`[Socket.IO] ${socket.data.userId} joined project:${projectId}`);
        } catch {
            socket.emit('error', { message: 'Erro ao entrar na sala do projeto' });
        }
    });
    ```

    **Passo D — Remover TODOS os handlers de relay (SEC-03):**
    Deletar completamente os seguintes blocos (não comentar — deletar):
    - `socket.on('task-updated', ...)` e seu corpo
    - `socket.on('task-moved', ...)` e seu corpo
    - `socket.on('task-created', ...)` e seu corpo
    - `socket.on('task-deleted', ...)` e seu corpo
    - `socket.on('comment-added', ...)` e seu corpo

    Manter apenas:
    - `socket.on('join-project', ...)` (versão nova com membership check)
    - `socket.on('leave-project', ...)` (pode permanecer, não tem risco de relay)
    - `socket.on('disconnect', ...)`

    CRÍTICO: NÃO usar `socket.data.userId` antes do `io.use()` ser executado. O middleware `io.use()` garante que `socket.data.userId` existe para todos os handlers dentro de `io.on('connection')`.
  </action>
  <verify>
    <automated>grep -n "io.use\|task-updated\|task-moved\|task-created\|task-deleted\|comment-added\|projectMember.findFirst\|socket.data.userId" apps/api/src/server.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "io.use(" apps/api/src/server.ts` retorna exatamente 1 resultado com o middleware de auth
    - `grep "socket.data.userId" apps/api/src/server.ts` retorna ao menos 1 resultado (no io.use e no join-project)
    - `grep "projectMember.findFirst" apps/api/src/server.ts` retorna 1 resultado (no join-project handler)
    - `grep "'task-updated'" apps/api/src/server.ts` retorna ZERO resultados (relay removido)
    - `grep "'task-moved'" apps/api/src/server.ts` retorna ZERO resultados
    - `grep "'task-created'" apps/api/src/server.ts` retorna ZERO resultados
    - `grep "'task-deleted'" apps/api/src/server.ts` retorna ZERO resultados
    - `grep "'comment-added'" apps/api/src/server.ts` retorna ZERO resultados
    - `grep "leave-project" apps/api/src/server.ts` retorna 1 resultado (handler mantido)
  </acceptance_criteria>
  <done>io.use() auth middleware presente; join-project verifica membership via Prisma; todos os 5 relay handlers removidos</done>
</task>

</tasks>

<verification>
Após execução dos dois tasks:

1. Compilação sem erros TypeScript:
   ```
   cd apps/api && npx tsc --noEmit
   ```
   (pode haver erros pré-existentes — verificar que nenhum NOVO erro foi introduzido nos arquivos modificados)

2. Verificação estrutural do server.ts:
   ```bash
   grep -c "io.use(" apps/api/src/server.ts  # deve retornar 1
   grep -c "projectMember.findFirst" apps/api/src/server.ts  # deve retornar 1
   grep -c "task-updated\|task-moved\|task-created\|task-deleted\|comment-added" apps/api/src/server.ts  # deve retornar 0
   grep -c "helmet" apps/api/src/server.ts  # deve retornar >= 2 (import + uso)
   ```

3. Inicialização da API (teste manual opcional):
   ```
   cd apps/api && npx tsx src/server.ts
   ```
   Deve inicializar sem erros de import. Ctrl+C para encerrar.
</verification>

<success_criteria>
- Helmet instalado e `app.use(helmet({contentSecurityPolicy: false, crossOriginEmbedderPolicy: false}))` antes de cors()
- `io.use()` presente com lógica JWT que popula `socket.data.userId`
- `join-project` verifica `prisma.projectMember.findFirst` antes de `socket.join()`
- Handlers task-updated, task-moved, task-created, task-deleted, comment-added: ausentes do arquivo
- SEC-01, SEC-02, SEC-03 (relay removal), SEC-04 todos implementados
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-security-hardening/01-01-SUMMARY.md` com:
- Arquivos modificados e o que foi feito em cada um
- Comando de instalação usado (npm install helmet)
- Confirmação de que os 5 relay handlers foram removidos
- Status de cada requirement: SEC-01, SEC-02, SEC-03 (relay), SEC-04
</output>
