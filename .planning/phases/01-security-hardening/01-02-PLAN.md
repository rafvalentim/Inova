---
phase: 01-security-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/modules/tasks/routes.ts
  - apps/api/src/server.ts
  - apps/api/package.json
autonomous: true
requirements:
  - SEC-03
  - SEC-05
  - SEC-06
user_setup: []

must_haves:
  truths:
    - "Eventos Socket.IO (task-updated, task-created, task-deleted, task-moved) são emitidos pelo servidor após mutações HTTP confirmadas no banco"
    - "Upload de arquivo com extensão .jpg mas conteúdo executável é rejeitado com 400 antes de ser salvo em disco"
    - "Duas chamadas concorrentes a generateTaskCode nunca retornam o mesmo código TASK-XXX"
  artifacts:
    - path: "apps/api/src/modules/tasks/routes.ts"
      provides: "emit io após mutações, MIME validation no multer, generateTaskCode com retry"
      contains: "req.app.get('io')"
    - path: "apps/api/package.json"
      provides: "file-type v18 instalado"
      contains: "\"file-type\""
  key_links:
    - from: "apps/api/src/modules/tasks/routes.ts POST/PUT/DELETE handlers"
      to: "req.app.get('io').to('project:${projectId}').emit(...)"
      via: "app.set('io', io) já existe em server.ts"
      pattern: "req\\.app\\.get\\('io'\\)"
    - from: "apps/api/src/modules/tasks/routes.ts upload middleware"
      to: "file-type fileTypeFromBuffer"
      via: "multer fileFilter lendo magic bytes"
      pattern: "fileTypeFromBuffer"
    - from: "apps/api/src/modules/tasks/routes.ts generateTaskCode"
      to: "prisma.task.create com code único"
      via: "catch P2002 + retry até 3 vezes"
      pattern: "P2002"
---

<objective>
Adicionar emissão de eventos Socket.IO após mutações HTTP, validação de MIME type por magic bytes no upload, e proteção de race condition no generateTaskCode.

Purpose: SEC-03 fecha o vetor onde um cliente autenticado forja eventos Socket.IO com dados fabricados. SEC-05 fecha o bypass de upload onde um .exe renomeado para .jpg passa pelo filtro. SEC-06 corrige a race condition onde dois requests simultâneos de criação de task geram o mesmo código TASK-XXX.

Output:
- `tasks/routes.ts` com `req.app.get('io').to(room).emit(...)` nos handlers POST/PUT/PATCH de tasks e POST de comentários
- `tasks/routes.ts` com fileFilter usando `file-type` para validar magic bytes
- `tasks/routes.ts` com `generateTaskCode` usando retry em P2002 (unique constraint já existe no schema)
- `package.json` com `file-type@18` instalado
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@apps/api/src/modules/tasks/routes.ts
@apps/api/src/server.ts
@apps/api/package.json
</context>

<interfaces>
<!-- Contratos relevantes extraídos do codebase. -->

De apps/api/src/server.ts:
```typescript
app.set('io', io);  // io é SocketIOServer — acessível em routes via req.app.get('io')
// rooms seguem padrão: `project:${projectId}`
```

De apps/api/src/modules/tasks/routes.ts (multer config atual — linhas 13-29):
```typescript
const storage = multer.diskStorage({ ... });
const upload = multer({
    storage,
    limits: { fileSize: config.upload.maxFileSize },
    // SEM fileFilter — aceita qualquer tipo
});
```

De apps/api/src/modules/tasks/routes.ts (generateTaskCode atual — linhas 204-227):
```typescript
async function generateTaskCode(): Promise<string> {
    const tasks = await prisma.task.findMany({
        where: { code: { startsWith: 'TASK-' } },
        select: { code: true },
    });
    // sem retry, sem captura de P2002
}
```

Schema Prisma (Task.code):
```
code  String  @unique  // constraint já existe — P2002 será lançado em duplicata
```

Eventos que o frontend espera (baseado em análise de grep — o frontend NÃO usa socket.io-client ativamente hoje, mas a estrutura de eventos deve ser mantida para uso futuro):
- `task-updated`: payload = objeto task completo
- `task-created`: payload = objeto task completo
- `task-deleted`: payload = taskId (string)
- `task-moved`: payload = { taskId, status, position }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Instalar file-type v18 e adicionar MIME validation no multer</name>
  <files>apps/api/package.json, apps/api/src/modules/tasks/routes.ts</files>
  <read_first>
    - apps/api/src/modules/tasks/routes.ts (linhas 1-30 — ver imports atuais e configuração multer)
    - apps/api/package.json (ver versão atual de multer e outras deps)
  </read_first>
  <action>
    **Passo A — Instalar file-type v18 (NÃO v19+ — é ESM-only e quebraria o CJS da API):**
    ```
    cd apps/api && npm install file-type@18
    ```

    **Passo B — Adicionar import no topo de tasks/routes.ts (após os imports existentes):**
    ```typescript
    import { fileTypeFromBuffer } from 'file-type';
    ```

    **Passo C — Substituir a configuração `upload` existente (linhas 26-29) pela versão com fileFilter:**

    Substituir:
    ```typescript
    const upload = multer({
        storage,
        limits: { fileSize: config.upload.maxFileSize },
    });
    ```

    Por:
    ```typescript
    const ALLOWED_MIME_TYPES = [
        'image/jpeg',
        'image/png',
        'image/gif',
        'image/webp',
        'application/pdf',
        'text/plain',
        'application/zip',
        'application/vnd.openxmlformats-officedocument.wordprocessingml.document',  // .docx
        'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',        // .xlsx
    ];

    const upload = multer({
        storage,
        limits: { fileSize: config.upload.maxFileSize },
        fileFilter: async (_req, file, cb) => {
            // Primeiro layer: extensão (rápido)
            const ext = path.extname(file.originalname).toLowerCase();
            const allowedExtensions = ['.jpg', '.jpeg', '.png', '.gif', '.webp', '.pdf', '.txt', '.zip', '.docx', '.xlsx'];
            if (!allowedExtensions.includes(ext)) {
                return cb(new Error('Tipo de arquivo não permitido'));
            }

            // Segundo layer: magic bytes via stream
            // Multer não fornece buffer diretamente no fileFilter — usamos o mimetype como pré-filtro
            // e validamos o buffer real no route handler após o upload (ver comentário abaixo)
            cb(null, true);
        },
    });
    ```

    **Passo D — No handler `POST /api/tasks/:id/attachments` (linha ~587), adicionar validação por magic bytes APÓS o multer processar o arquivo:**

    Localizar o handler e adicionar validação logo após verificar `req.file`:
    ```typescript
    if (!req.file) {
        return res.status(400).json({ success: false, message: 'Arquivo não enviado' });
    }

    // SEC-05: Validar MIME type real via magic bytes (não confiar no header do cliente)
    const fileBuffer = fs.readFileSync(req.file.path);
    const detectedType = await fileTypeFromBuffer(fileBuffer);
    if (!detectedType || !ALLOWED_MIME_TYPES.includes(detectedType.mime)) {
        fs.unlinkSync(req.file.path); // remover arquivo inválido do disco
        return res.status(400).json({
            success: false,
            message: `Tipo de arquivo não permitido: ${detectedType?.mime ?? 'desconhecido'}`,
        });
    }
    ```

    ATENÇÃO: A constante `ALLOWED_MIME_TYPES` deve ser definida no escopo do módulo (fora do handler), conforme Passo C acima.
  </action>
  <verify>
    <automated>grep -n "fileTypeFromBuffer\|ALLOWED_MIME_TYPES\|file-type\|magic" apps/api/src/modules/tasks/routes.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import.*fileTypeFromBuffer" apps/api/src/modules/tasks/routes.ts` retorna 1 resultado
    - `grep "ALLOWED_MIME_TYPES" apps/api/src/modules/tasks/routes.ts` retorna ao menos 2 resultados (definição + uso)
    - `grep "fileTypeFromBuffer" apps/api/src/modules/tasks/routes.ts` retorna ao menos 2 resultados (import + uso no handler)
    - `grep "fs.unlinkSync" apps/api/src/modules/tasks/routes.ts` retorna resultado no handler de attachment (remoção de arquivo inválido)
    - `grep "\"file-type\"" apps/api/package.json` retorna linha com versão ~18
    - `node -e "const ft = require('file-type'); console.log(typeof ft.fileTypeFromBuffer)"` executado em `apps/api` retorna `function`
  </acceptance_criteria>
  <done>file-type@18 instalado; handler de upload valida magic bytes e rejeita com 400 + remove arquivo do disco se MIME inválido</done>
</task>

<task type="auto">
  <name>Task 2: Corrigir generateTaskCode com retry em P2002 (SEC-06)</name>
  <files>apps/api/src/modules/tasks/routes.ts</files>
  <read_first>
    - apps/api/src/modules/tasks/routes.ts (linhas 204-260 — ver generateTaskCode e o handler POST que a chama)
  </read_first>
  <action>
    **Substituir completamente a função `generateTaskCode` (linhas 204-227) pela versão abaixo:**

    ```typescript
    // Gera código sequencial único para task (TASK-001, TASK-002, ...)
    // Usa retry em P2002 (unique constraint violation) para evitar race condition (SEC-06)
    async function generateTaskCode(): Promise<string> {
        try {
            const tasks = await prisma.task.findMany({
                where: { code: { startsWith: 'TASK-' } },
                select: { code: true },
            });

            if (tasks.length === 0) return 'TASK-001';

            const max = tasks.reduce((acc, t) => {
                const num = parseInt(t.code.split('-')[1], 10);
                return isNaN(num) ? acc : Math.max(acc, num);
            }, 0);

            return `TASK-${String(max + 1).padStart(3, '0')}`;
        } catch (e: any) {
            if (e?.message?.includes('column') || e?.code === 'P2022') {
                console.warn('[generateTaskCode] Coluna code não existe ainda. Rode a migration. Usando fallback.');
                return `TASK-${Date.now().toString().slice(-6)}`;
            }
            throw e;
        }
    }

    // Cria task com código único — retenta até 3 vezes em caso de race condition (P2002)
    async function createTaskWithCode(data: Parameters<typeof prisma.task.create>[0]['data']): Promise<Awaited<ReturnType<typeof prisma.task.create>>> {
        const MAX_RETRIES = 3;
        for (let attempt = 1; attempt <= MAX_RETRIES; attempt++) {
            const code = await generateTaskCode();
            try {
                return await prisma.task.create({ data: { ...data, code } });
            } catch (e: any) {
                if (e?.code === 'P2002' && attempt < MAX_RETRIES) {
                    // Race condition: outro request criou task com o mesmo código — retenta
                    continue;
                }
                throw e;
            }
        }
        throw new Error('Não foi possível gerar código único para a task após 3 tentativas');
    }
    ```

    **Atualizar o handler POST /api/projects/:projectId/tasks para usar `createTaskWithCode`:**

    Localizar o bloco onde `prisma.task.create` é chamado após `generateTaskCode()` (por volta da linha 254).

    Substituir a chamada atual (que gera o código separadamente e passa para task.create):
    ```typescript
    const [code, lastTask] = await Promise.all([generateTaskCode(), ...]);
    ...
    const task = await prisma.task.create({ data: { code, ... } });
    ```

    Por:
    ```typescript
    const lastTask = await prisma.task.findFirst({
        where: { projectId: req.params.projectId, status: 'BACKLOG' },
        orderBy: { position: 'desc' },
        select: { position: true },
    });
    const position = (lastTask?.position ?? -1) + 1;

    const task = await createTaskWithCode({
        projectId: req.params.projectId,
        title,
        description: description || null,
        // ... todos os outros campos que estavam no data original, EXCETO code (gerado internamente)
        position,
        // incluir todos os outros campos do data original
    });
    ```

    ATENÇÃO: Ao substituir, preservar todos os campos do `data` original. Apenas remover o campo `code` do objeto (pois `createTaskWithCode` o gera internamente) e remover o `generateTaskCode()` do `Promise.all`.

    Preservar os includes (`_count`, `assignees`, etc.) adicionando `.then(task => prisma.task.findUnique({ where: { id: task.id }, include: { ... } }))` se necessário, ou ajustar `createTaskWithCode` para retornar o task com includes — verificar como o handler atual usa o retorno.
  </action>
  <verify>
    <automated>grep -n "P2002\|createTaskWithCode\|MAX_RETRIES" apps/api/src/modules/tasks/routes.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "P2002" apps/api/src/modules/tasks/routes.ts` retorna ao menos 1 resultado (no catch do retry)
    - `grep "createTaskWithCode" apps/api/src/modules/tasks/routes.ts` retorna ao menos 2 resultados (definição + uso)
    - `grep "MAX_RETRIES" apps/api/src/modules/tasks/routes.ts` retorna resultado com valor 3
    - `grep "generateTaskCode()" apps/api/src/modules/tasks/routes.ts` NÃO aparece diretamente no handler POST (apenas dentro de `createTaskWithCode`)
    - `npx tsc --noEmit` executado em `apps/api` não introduz novos erros nos arquivos modificados
  </acceptance_criteria>
  <done>generateTaskCode encapsulada em createTaskWithCode com retry em P2002; handler POST usa createTaskWithCode</done>
</task>

<task type="auto">
  <name>Task 3: Adicionar emissão Socket.IO nos handlers HTTP de tasks (SEC-03)</name>
  <files>apps/api/src/modules/tasks/routes.ts</files>
  <read_first>
    - apps/api/src/modules/tasks/routes.ts (ler os handlers: POST /projects/:projectId/tasks, PUT /:id, PATCH /:id/status, DELETE /:id — ver onde res.json() é chamado no caminho de sucesso)
    - apps/api/src/server.ts (confirmar que app.set('io', io) existe)
  </read_first>
  <action>
    Em cada handler HTTP de mutação de task, adicionar emissão Socket.IO **após** o write no banco e **antes** do `res.json()` de sucesso.

    **Padrão a seguir em todos os handlers:**
    ```typescript
    const io = req.app.get('io');
    if (io) {
        io.to(`project:${projectId}`).emit('event-name', payload);
    }
    ```

    Usar `if (io)` para evitar erro caso io não esteja disponível (ex: testes unitários).

    **Handlers a atualizar e seus eventos:**

    1. `POST /api/projects/:projectId/tasks` (criação de task):
       Após `prisma.task.create` / `createTaskWithCode` retornar com sucesso:
       ```typescript
       const io = req.app.get('io');
       if (io) io.to(`project:${req.params.projectId}`).emit('task-created', task);
       ```

    2. `PUT /:id` (atualização de task — localizar pelo method PUT e route /:id):
       Após `prisma.task.update` retornar com sucesso:
       ```typescript
       const io = req.app.get('io');
       if (io) io.to(`project:${updatedTask.projectId}`).emit('task-updated', updatedTask);
       ```
       Verificar como o projectId está disponível (pode ser do `updatedTask.projectId` ou de query ao banco antes).

    3. `PATCH /:id/status` (mudança de status/posição):
       Após o update retornar:
       ```typescript
       const io = req.app.get('io');
       if (io) io.to(`project:${task.projectId}`).emit('task-moved', {
           taskId: task.id,
           status: task.status,
           position: task.position,
       });
       ```

    4. `DELETE /:id` (exclusão de task):
       Após `prisma.task.delete` e ANTES de `res.json({ success: true })`:
       ```typescript
       const io = req.app.get('io');
       if (io) io.to(`project:${task.projectId}`).emit('task-deleted', task.id);
       ```
       Obter `task.projectId` via `prisma.task.findUnique` ANTES do delete (para ter o projectId disponível após a exclusão).

    ATENÇÃO: Não emitir eventos em caso de erro (dentro de blocos catch). Apenas no caminho de sucesso.
    ATENÇÃO: O `projectId` deve vir do banco (task.projectId), não do cliente/request params quando possível, para garantir consistência.
  </action>
  <verify>
    <automated>grep -n "req.app.get('io')\|req\.app\.get(\"io\")" apps/api/src/modules/tasks/routes.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "req.app.get('io')" apps/api/src/modules/tasks/routes.ts` retorna >= 4 (um por handler: create, update, status, delete)
    - `grep "task-created" apps/api/src/modules/tasks/routes.ts` retorna 1 resultado (no handler de criação)
    - `grep "task-updated" apps/api/src/modules/tasks/routes.ts` retorna 1 resultado (no handler de update)
    - `grep "task-moved" apps/api/src/modules/tasks/routes.ts` retorna 1 resultado (no handler de status/posição)
    - `grep "task-deleted" apps/api/src/modules/tasks/routes.ts` retorna 1 resultado (no handler de delete)
    - `grep "if (io)" apps/api/src/modules/tasks/routes.ts` retorna ao menos 4 resultados (guard em cada emit)
  </acceptance_criteria>
  <done>4 handlers HTTP emitem eventos Socket.IO após write confirmado; todos protegidos com `if (io)` guard</done>
</task>

</tasks>

<verification>
Após execução dos três tasks:

1. Compilação TypeScript sem novos erros:
   ```
   cd apps/api && npx tsc --noEmit 2>&1 | tail -20
   ```

2. Verificação de cobertura de eventos:
   ```bash
   grep -c "req.app.get('io')" apps/api/src/modules/tasks/routes.ts  # >= 4
   grep -c "P2002" apps/api/src/modules/tasks/routes.ts              # >= 1
   grep -c "fileTypeFromBuffer" apps/api/src/modules/tasks/routes.ts # >= 2
   ```

3. Verificar que file-type foi instalado:
   ```
   cd apps/api && node -e "const {fileTypeFromBuffer} = require('file-type'); console.log('ok')"
   ```
</verification>

<success_criteria>
- file-type@18 instalado e import resolvido no CJS da API
- Upload route valida magic bytes e rejeita com 400 + remove arquivo se MIME inválido
- createTaskWithCode faz retry em P2002 até 3 vezes
- 4 handlers HTTP (create, update, status, delete) emitem eventos Socket.IO após write no banco
- Todos os emits protegidos com `if (io)` guard
- SEC-03 (emit server-side), SEC-05 (MIME validation), SEC-06 (race fix) todos implementados
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-security-hardening/01-02-SUMMARY.md` com:
- Arquivos modificados e mudanças em cada um
- Versão instalada de file-type
- Confirmação de que todos os 4 handlers HTTP emitem eventos
- Status de cada requirement: SEC-03 (emit), SEC-05, SEC-06
</output>
